[tool.poetry]
name = "langgraph-agent"
version = "0.1.0"
description = "A simple LangGraph agent example"
requires-python = ">=3.9"

[tool.poetry.dependencies]
python = ">=3.9"
langgraph = ">=0.2.0"
langchain = ">=0.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
