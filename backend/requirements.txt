# 依赖管理说明
# 本项目使用分离的依赖文件管理策略：
# 1. requirements-core.txt - 核心依赖（轻量级）
# 2. requirements-big.txt - 大体积依赖（重量级）
# 
# 这样做的好处：
# - 利用Docker层缓存，提高构建速度
# - 减少镜像拉取时间
# - 便于依赖管理和版本控制

# 核心依赖（轻量级）
-r requirements-core.txt

# 大体积依赖（重量级）
-r requirements-big.txt
